package com.rogertalk.roger.models.data

enum class ConversationOption {
    SET_NAME,
    MUTE_CONVERSATION,
    UNMUTE_CONVERSATION,
    LEAVE_GROUP,
    MEMBERS,
    ATTACHMENT_PICTURE,
    ATTACHMENT_LINK_FROM_CLIPBOARD,
    TALKHEAD,
    MORE
}