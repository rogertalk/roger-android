<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/blackCircle"
        android:layout_width="@dimen/contact_circle_diameter"
        android:layout_height="@dimen/contact_circle_diameter"
        android:importantForAccessibility="no"
        android:src="@drawable/circle_black"/>

    <ImageView
        android:id="@+id/notificationContactImage"
        android:layout_width="@dimen/contact_circle_diameter"
        android:layout_height="@dimen/contact_circle_diameter"
        android:importantForAccessibility="no"
        android:src="@drawable/circle_black" />

    <TextView
        android:id="@+id/notificationInitials"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:fontFamily="sans-serif"
        tools:text="R"
        android:textAllCaps="true"
        android:textColor="@color/opaque_white"
        android:textSize="@dimen/font_xxxl" />

    <!-- NOTE:  Leave the R in the text above, it helps initializing draw measurements since this layout
     is not rendered the usual way -->

</RelativeLayout>